{
  "name": "seu-projeto",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Building the project...' && your-build-command",
    "test": "echo 'Running tests...' && your-test-command"
  },
  "dependencies": {
    "your-dependency": "^1.0.0"
  }
}